# Q-Prime Docker Configuration
# 1. Fill in the required values below
# 2. Run: ./docker/scripts/setup.sh
#
# setup.sh generates and manages everything else automatically.

# ── Required ──────────────────────────────────────────────────────────────────

# Your deployment domain
DOMAIN=yourdomain.edu
LETSENCRYPT_EMAIL=admin@yourdomain.edu
AUTH_GOOGLE_ID=your-client-id.apps.googleusercontent.com
AUTH_GOOGLE_SECRET=your-client-secret


# ── Optional ──────────────────────────────────────────────────────────────────

# API path prefix — change if running multiple deployments on one host
# HTTP_API_PREFIX=/api

# Set to 1 to use Let's Encrypt staging server (avoids rate limits while testing)
# LETSENCRYPT_STAGING=0

# Deployment mode. Default "true" is single-OHQ (root redirects to your one
# course; no landing page). Set to "false" to host the public landing page
# instead (multi-OHQ mode, for hosting many courses).
# VITE_SINGLE_COURSE_MODE=true
